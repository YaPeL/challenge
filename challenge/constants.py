"""
Special file used to store the constants used through the project
"""
from typing import Union

ARRAY_START = "["

ARRAY_END = "]"

DEFAULT_ENCODING = "utf-8"

JSON_PROPERTY = Union[str, int, float, dict, list, bool, None]

PROPERTY_SEPARATOR = "."
